<?php

namespace App\Http\Controllers;

/**
 * TransactionDetails are managed internally via TransactionsController
 * (borrow) and ReturnService (return). This controller is intentionally
 * empty — kept only to satisfy the file structure convention.
 */
class TransactionDetailsController extends Controller
{
    //
}
